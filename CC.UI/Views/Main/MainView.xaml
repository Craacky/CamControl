<UserControl x:Class="CC.UI.Views.Main.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:main="clr-namespace:CC.UI.ViewModels.Main"
             xmlns:main1="clr-namespace:CC.UI.Controls.Main"
             xmlns:external="clr-namespace:CC.UI.Controls.External"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=main:MainViewModel, IsDesignTimeCreatable=False}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../Styles/TextBlock.xaml" />
                <ResourceDictionary Source="../../Styles/Border.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{StaticResource ViewBorderStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="Главная"
                       Style="{StaticResource HeaderTextBlockStyle}" />

            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="0.2*" />
                    <RowDefinition Height="0.5*" />
                    <RowDefinition Height="0.7*" />
                    <RowDefinition />
                </Grid.RowDefinitions>

                <main1:DevicesControl Grid.Row="0" DataContext="{Binding DeviceService}" />
                <external:CurrentTaskControl Grid.Row="1" DataContext="{Binding ReportTaskService.CurrentReportTask}" />

                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                        <ColumnDefinition />
                        <ColumnDefinition />
                        <ColumnDefinition Width="1.2*" />
                    </Grid.ColumnDefinitions>

                    <main1:ProductStatisticControl Grid.Column="0" VerticalAlignment="Top" />
                    <main1:BoxStatisticControl Grid.Column="1" VerticalAlignment="Top" />
                    <main1:PalletStatisticControl Grid.Column="2" VerticalAlignment="Top" />
                    <main1:CurrentPalletStatisticControl Grid.Column="3" VerticalAlignment="Top" />
                    <main1:ButtonsPallet Grid.Column="4" VerticalAlignment="Top" />
                </Grid>
            </Grid>

            <external:CameraResultTableControl Grid.Row="3" />
        </Grid>
    </Border>
</UserControl>