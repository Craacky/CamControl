<UserControl x:Class="CC.UI.Views.Main.ReportTasksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:main="clr-namespace:CC.UI.ViewModels.Main"
             xmlns:external="clr-namespace:CC.UI.Controls.External"
             xmlns:reportTask="clr-namespace:CC.UI.Controls.ReportTask"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=main:ReportTasksViewModel, IsDesignTimeCreatable=False}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../Styles/TextBlock.xaml"/>
                <ResourceDictionary Source="../../Styles/Border.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{StaticResource ViewBorderStyle}">
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0"
                       Text="Задания"
                       Style="{StaticResource HeaderTextBlockStyle}"/>

            <external:CurrentTaskControl Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                                               DataContext="{Binding ReportTaskService.CurrentReportTask}"/>

            <reportTask:TableControl Grid.Row="2" Grid.Column="0"/>
            <reportTask:ButtonsControl Grid.Row="2" Grid.Column="1"/>
        </Grid>
    </Border>
</UserControl>
