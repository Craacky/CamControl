<UserControl x:Class="CC.UI.Views.MainWindow.TasksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CC.UI.Views.MainWindow"
             xmlns:common="clr-namespace:CC.UI.Controls.Common"
             xmlns:taskView="clr-namespace:CC.UI.Controls.TaskView"
             xmlns:mainWindow="clr-namespace:CC.UI.ViewModels.MainWindow"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=mainWindow:TasksViewModel, IsDesignTimeCreatable=False}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../Styles/TextBlock.xaml"/>
                <ResourceDictionary Source="../../Styles/Border.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{StaticResource ViewBorderStyle}">
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0"
                       Text="Задания"
                       Style="{StaticResource HeaderTextBlockStyle}"/>

            

            <common:CurrentReportTaskControl Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                                               DataContext="{Binding ReportTaskService.CurrentReportTask}"/>

            <taskView:TaskTableControl Grid.Row="2" Grid.Column="0"/>
            <taskView:TaskButtonsControl Grid.Row="2" Grid.Column="1"/>
        </Grid>
    </Border>
</UserControl>
